import React from 'react';

import './firebase-auth-react.scss';

/* eslint-disable-next-line */
export interface FirebaseAuthReactProps {}

export const FirebaseAuthReact = (props: FirebaseAuthReactProps) => {
  return (
    <div>
      <h1>Welcome to firebase-auth-react component!</h1>
    </div>
  );
};

export default FirebaseAuthReact;
